# Railway PostgreSQL connection string.
# Railway -> Postgres service -> "Connect" -> copy DATABASE_URL (public proxy URL for local dev).
DATABASE_URL=postgresql://postgres:password@host.proxy.rlwy.net:5432/railway

# Origin allowed by CORS (your frontend dev server).
FRONTEND_ORIGIN=http://localhost:5173
